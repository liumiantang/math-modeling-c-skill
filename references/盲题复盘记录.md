# 盲题复盘记录

每道陌生题独立填写，先保存初始判断，再填写最终结果；不要用事后结论覆盖最初思路。

## 基本信息

- 题目：
- 日期：
- 用时：
- 数据和代码目录：
- MATLAB 运行入口：
- 最终采用模型：

## A. 初始判断

- 我判断的题型：
- 判断依据：
- 机制图中的关键箭头：
- 我认为最重要的约束：
- 我最担心的风险：

## B. 候选模型竞争

| 模型 | 预期优点 | 实际指标 | 失败/风险 | 是否保留 |
|---|---|---|---|---|
| 基线 |  |  |  |  |
| 主模型 |  |  |  |  |
| 风险模型 |  |  |  |  |

记录验证集、测试集、滚动窗口、最坏情景、敏感性和方案一致率，不只记录最终目标值。

## C. 反例攻击

- 改变哪个参数后结论最敏感：
- 删除哪个字段后模型失效：
- 哪个约束最可能与其他约束冲突：
- 是否发现数据泄漏、自由变量或求解器状态误读：
- 简单基线是否在某些场景更好：

## D. 判断误差

- 初始判断错在哪里：
- 哪个早期信号本来可以发现错误：
- 是题意误读、数据误读、模型选择错，还是验证不足：
- 如果重新开始，我会先做什么：

## E. 可迁移规则

把本题经验改写成不依赖具体题目的规则：

1. 当出现 `________` 时，先检查 `________`。
2. 如果 `________`，不能直接声称 `________`。
3. 在 `________` 场景下，应比较 `________` 与 `________`。

## F. 交付审计

- [ ] MATLAB 静态检查和实际运行均通过
- [ ] 结果独立重算且约束残差合格
- [ ] 数字可追溯到结果文件
- [ ] 敏感性/最坏情景/稳定性已做
- [ ] 未把相关写成因果
- [ ] 未把局部、情景或有限枚举结果写成全局最优
