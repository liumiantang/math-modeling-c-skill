---
name: math-modeling-c-problem-solving
description: 面向数学建模竞赛 C 类题目的机制识别、模型竞争、MATLAB 验证、误差与敏感性分析、优化审计和论文复盘工作流。适用于用户要求分析、建模、复现、验证或完善 C 类赛题时。
---

# 数学建模 C 题建模协作 Skill

## 适用目标

把 C 题求解从“套一个熟悉模型”提升为可审计的决策流程：先判断题意机制和数据能否支持结论，再选择候选模型，用 MATLAB 实际运行和独立复核，最后沉淀为论文中的可解释结论和可迁移判断规则。

## 总体工作流

1. **题意与机制图**
   - 区分观测、控制、状态、结果和决策变量。
   - 画出“输入数据 → 机制/中间量 → 预测或约束 → 决策目标”的因果/流程图。
   - 先回答题目到底要求相关性、预测、分类、调度、资源分配，还是不确定性下的策略。
   - 检查题目中的阈值、时间顺序、重复观测、缺货、残值、替代、容量和供应商限制。

2. **数据审计**
   - 核对文件、表名、行列数、字段含义、单位、重复主体和缺失模式。
   - 检查标签是否真的可用；空白不自动等于正常，健康字段若只有一个取值不能作为分类标签。
   - 时间序列按时间排序；重复受试者或重复商品不能随机拆入训练集和测试集。
   - 记录所有清洗规则，保留原始值、清洗后值和被剔除记录数。

3. **候选模型竞争**
   - 至少建立一个可解释基线和一个机制更强的候选模型。
   - 预测：朴素基线、移动/季节基线、回归或时序模型；用滚动时间窗比较。
   - 分类：正则 Logistic、树模型或其他候选；按主体分层交叉验证，类别不平衡时报告 PR-AUC。
   - 网络/调度：贪心或规则基线与 LP/MILP/最小费用流竞争。
   - 优化：名义模型与情景/鲁棒模型竞争，并报告上下界、可行性和最优性间隙。

4. **模型定义必须完整**
   每个模型都写清：
   - 假设及其理由；
   - 集合、参数、变量及单位；
   - 目标函数及每一项的业务含义；
   - 等式、不等式、边界、逻辑和可行性约束；
   - 哪些部分是题目给定，哪些部分是人为假设。

5. **MATLAB 验证**
   - 优先使用 MATLAB MCP，不用 Python 替代 MATLAB 验证。
   - 先用 `check_matlab_code` 做静态检查，再用 `run_matlab_file` 实际运行。
   - 静态检查通过不代表模型正确；必须处理运行期的尺寸、空集合、索引、类型和求解器版本问题。
   - 优化模型求解后必须独立解码：重新计算收入、成本、产量、销量、约束残差、缺货、浪费和目标值。
   - 对整数规划记录 `exitflag`、时间限制、上下界、相对间隙和是否得到可行整数解。

6. **验证和风险评价**
   - 训练集、验证集、测试集严格隔离；时间序列使用滚动窗口。
   - 报 MAE、RMSE、R²、方向准确率或业务损失，不只报拟合优度。
   - 分类至少报告 ROC-AUC、PR-AUC、灵敏度、特异度、平衡准确率和 Bootstrap 区间。
   - 优化至少做参数敏感性、最坏情景、可行性审计和多随机种子稳定性。
   - 检查预测误差是否会改变最终决策，而不是只证明预测模型拟合得好。

7. **论文复盘**
   - 结果表中的数字必须直接来自 MATLAB 保存的结果文件。
   - 图表、正文、摘要和代码使用同一份数据和同一组参数。
   - 明确“相关不等于因果”；附件缺少识别条件时不能靠复杂模型补出因果结论。
   - 明确模型的适用范围、失败案例、参数依赖和未完成的全局性证明。

## 三类 C 题的判断规则

### 预测—决策

- 题目出现“预测后决定价格、补货、检测时机”时，目标不是单纯最小预测误差，而是最小化决策损失。
- 先建立预测模型，再把预测区间或误差分布传入决策模型。
- 阈值结果是样本和参数下的模型目标，不自动等于医学或管理规范。
- 受试者有多次记录时，标签和切分必须在受试者层面。

### 网络/调度

- 出现节点、流量、时间窗、配送、容量、先后关系或资源冲突时，优先画网络图或甘特逻辑。
- 能否跨期、能否等待、是否有保质期、是否允许缺货，决定弧集合和状态转移。
- 目标函数必须同时反映服务水平、等待/持有成本、浪费和违约成本。
- 网络策略在测试期更好时，仍需检查是否只是牺牲浪费换取缺货下降。

### 不确定性优化

- 不确定性来源要分类：需求、价格、产量、成本、时间、容量或标签误差。
- 先定义情景或区间，再决定使用情景规划、最坏情景、机会约束、CVaR 或鲁棒 counterpart。
- 空集合、无合法操作、未覆盖情景和自由销售变量是高风险漏洞。
- 鲁棒模型必须将每个情景的目标值独立重算，并比较名义策略和鲁棒策略的最坏收益与基准收益权衡。

## 已验证的项目经验

- 2025 NIPT：女胎“胎儿是否健康”字段全为“是”，不能作为二分类标签；异常标签存在主体内记录不一致，需按主体聚合并取首次检测特征。当前核心特征模型的 OOF 性能较弱，只能定位为探索性风险排序。
- 2023 分时补货派生案例：网络 LP 在留出日期上降低综合成本和缺货，但增加浪费；容量和罚损权重敏感，不能只展示一条最优配送方案。
- 2024 种植鲁棒扩展：若作物—季次没有合法操作，销售变量必须显式约束为0；求解器目标与独立解码目标不一致时，所有数值结论作废。
- 任意面积混种：受限列池整数值是可行下界，完整 LP 松弛值才可作为上界；分支定界未穷尽前不能写“全局最优”。

## 交付前检查清单

- [ ] 题意机制图和变量字典已完成。
- [ ] 数据行数、主体数、重复数、缺失和标签口径已审计。
- [ ] 每个模型都有假设、变量、目标和约束。
- [ ] 至少两个候选模型或基线已比较。
- [ ] MATLAB 静态检查通过，代码实际运行成功。
- [ ] 结果经过独立重算和可行性审计。
- [ ] 已做误差、敏感性、稳定性和最坏场景分析。
- [ ] 所有数字在代码、结果文件、图表和论文中一致。
- [ ] 不确定性、因果边界、求解时间限制和未完成证明已明确指出。



## 升级版执行协议

### 0. 先写“题目契约”，再写代码

在建模前先用一页文字固定以下内容：

- 题目要求的最终输出是什么，以及评价指标是什么；
- 决策发生在预测之前还是之后，是否允许跨期调整；
- 硬约束、软约束、题目给定参数和自行假设分别是什么；
- 数据能够支持的结论边界，以及明确不能识别的结论；
- 至少一个可解释基线和至少一个增强模型。

如果题目契约尚未完成，不得先运行优化器并把输出包装成结论。若多个小问共享数据，必须先建立共享变量、时间口径和单位字典，避免每问各自清洗后口径漂移。

### 1. 机制判断决策树

按以下顺序判断，不按熟悉的算法反推题型：

1. 是否存在未来值、滞后值、检测时机或价格/补货决定？有则进入“预测—决策”；
2. 是否存在节点、路径、流量、时间窗、先后关系或资源冲突？有则进入“网络/调度”；
3. 是否存在需求、价格、产量、容量或损耗的区间/情景？有则进入“不确定性优化”；
4. 是否有明确标签或阈值判定？有则进入“分类/风险分层”；
5. 若同时满足多个条件，拆成“统计估计层 → 决策优化层”，不要用一个黑箱模型吞掉所有问题。

每次判断都要回答：

- 题目中的机制箭头是什么，哪些箭头只是相关关系；
- 哪些量是观测量、状态量、控制量、随机量和最终决策量；
- 一个改变会通过什么路径影响目标函数；
- 哪些变量没有数据支持，因而只能做情景假设。

### 2. 候选模型的淘汰规则

模型竞争不是堆算法，而是比较“机制匹配度—可验证性—决策价值”：

- 基线模型必须能在数据不足时仍然运行，并说明其偏差来源；
- 增强模型必须解决基线的具体缺陷，而不是只增加复杂度；
- 若增强模型只改善训练集、不改善滚动验证或决策损失，应保留基线；
- 若模型更复杂但无法解释参数、无法审计约束或无法复现，不能仅凭更高拟合度采用；
- 预测题比较预测误差和业务损失，优化题比较目标、可行性、风险和稳定性，分类题不能只比较 Accuracy。

### 3. “模型结果护照”

每个最终采用的模型都要生成一个可追溯记录，至少包含：

```
problem_id, question_id, data_files, data_hash_or_timestamp
assumptions, variable_dictionary, parameter_units
objective_definition, constraints, solver_and_options
script_path, exact_run_entry, random_seeds
train_validation_test_rule, metrics, confidence_or_prediction_intervals
sensitivity_grid, worst_case_scenarios, feasibility_audit
result_file, figure_files, key_numbers, limitations, verified_status
```

`key_numbers` 必须由 MATLAB 结果文件导出，不能由论文作者手工二次抄录。若暂时没有自动生成护照，至少在项目目录中建立 `model_passport.md`，并把每个数字对应到脚本变量和结果文件。

### 4. MATLAB 执行与独立解码

- 优先通过 MATLAB MCP 执行：先静态检查，再运行脚本，再读取结果；
- 共享工作流目录为 `D:\Practice\matlab_c_workflow`，可复用其中的 `auditData`、`rollingBacktest`、`validateSolution` 和 `sensitivityGrid`；
- 每次正式运行保存参数、随机种子、求解器状态和结果文件；
- 优化器返回的目标值不能直接作为论文数字，必须用原始决策变量独立重算收入、成本、产量、缺货、浪费、库存和约束残差；
- 出现 `exitflag` 非成功、不可行、超时、空解、NaN、维度异常或结果与独立重算不一致时，停止写结论并先定位原因；
- 不能用 Python 的拟合或仿真结果冒充 MATLAB 验证。其他语言最多用于非数值性的文件整理，并须说明。

### 5. 决策可靠性检查

验证不止是“模型拟合得好”：

- 预测误差要传入后续决策，检查误差变化是否导致价格、订货、检测时机或选品改变；
- 优化要报告名义方案、保守方案和最坏情景的目标差异；
- 对关键参数做局部扰动、范围扫描和必要的联合扰动；
- 对随机算法或随机初始化重复多个种子，报告均值、标准差、最好/最差结果和方案一致率；
- 对分类阈值报告阈值变化下的灵敏度、特异度、阳性预测值和风险分层稳定性；
- 对小样本或标签不可靠的数据降低结论强度，不能用复杂模型制造虚假的确定性。

### 6. 与论文写作技能的接口

论文写作技能只消费已验证的模型护照、MATLAB 结果文件和 MATLAB 图表，不负责补造实验结果。写作时必须区分：

- 题目事实、数据事实、模型假设、计算结果和解释性推断；
- 相关性、预测能力、决策效果和因果结论；
- 可行解、局部最优、情景最优、上下界和已证明的全局最优。

国赛论文可使用“问题分析—模型假设—符号说明—模型建立—算法与实现—结果与检验—敏感性分析—优缺点与推广”的结构，但不能盲目套用期刊论文格式。摘要中的数字、正文数字、表格数字和图中文字必须来自同一份 MATLAB 结果快照。

## 高风险失败模式拦截

- 把缺失值、空白值或未观测值直接当成 0；
- 把训练期未来信息、主体内重复记录或同一商品的测试期信息泄漏进训练集；
- 把“相关性最高”写成“替代关系”或“因果影响”；
- 用均值容量、均值需求或单一情景制造必然可行的优化问题；
- 忽略无合法操作的索引，导致不存在的作物、路线、产品或供应商获得自由变量；
- 优化模型有自由销售量、自由库存量或未绑定的松弛变量；
- 只看求解器目标，不独立重算；
- 在分支定界、枚举或上下界没有完成时写“全局最优”；
- 用一组敏感性参数挑选有利结果，却不报告完整范围；
- 图表、代码、结果文件和论文使用不同清洗口径或不同参数。


## 工具分工经验：MATLAB—Visio—ARS

三类工具按职责分层，不互相替代：

```text
MATLAB MCP  → 数据计算、参数估计、优化求解、误差/敏感性/稳定性验证、定量图表
Visio MCP   → 机制图、流程图、网络图、模型结构图、论文示意图
ARS-Codex   → 论文结构、摘要与表述、引用核查、审稿复盘、格式整理
```

- MATLAB 生成的结果文件是唯一数字来源；Visio 图中的数值若有定量含义，也必须来自 MATLAB 结果快照。
- Visio 只表达机制和结构，不承担统计检验、优化求解或结果真实性验证。
- ARS 只消费已验证的模型护照、MATLAB 结果文件和图表，不得自行补写实验数字、显著性或最优性结论。
- 机制图应先于正式建模完成；流程图至少标出数据输入、机制/中间量、决策变量、目标和验证反馈。
- 使用 Visio MCP 时优先调用标准模板和标准图形，再连接形状；保存 `.vsdx` 后读取页面摘要或重新打开文件检查形状数和连接数。
- 最终论文图表可由 MATLAB 和 Visio 分工生成，但文件名、版本、正文引用和结果护照必须互相对应。

## Visio MCP 实测经验

本项目已通过 Visio MCP 的标准 `flowchart` 模板创建数学建模流程图，批量绘制 6 个节点、5 条连接，成功保存 `.vsdx` 并导出 PNG。由此固化以下判断：Visio MCP 适合机制表达、流程梳理和论文示意图；MATLAB MCP 负责数值计算与验证；ARS-Codex 负责把已验证材料组织成论文。不能让 Visio 图形替代模型审计，也不能让论文技能补造 MATLAB 结果。


## 机制洞察与创新建模训练闭环

当题目是陌生题、机制不清或需要创新建模时，先读取并填写以下三份参考模板：

- [题意机制卡模板](references/题意机制卡模板.md)：在写代码前识别对象、状态、控制、随机量、作用路径和可识别边界；
- [创新点生成与筛选规则](references/创新点生成与筛选规则.md)：从题目矛盾中提出创新，而不是无目的堆叠算法；
- [盲题复盘记录](references/盲题复盘记录.md)：记录判断、反例攻击、模型淘汰和赛后修正，形成可迁移经验。

每道训练题至少完成一次“机制卡 → 基线/主模型/风险模型竞争 → MATLAB 验证 → 反例攻击 → 复盘”。如果机制卡无法说明变量之间的作用路径，或者创新点无法对应题目中的具体矛盾，应降低模型复杂度并先补充题意分析。
